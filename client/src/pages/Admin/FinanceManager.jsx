import { API_URL } from '../../utils/api';
import React, { useState, useEffect } from 'react'; import axios from 'axios'; import { DollarSign, TrendingUp, CreditCard, Activity, ArrowUpRight, Download } from 'lucide-react';  const FinanceManager = () => {   const [orders, setOrders] = useState([]);   const [loading, setLoading] = useState(true);    useEffect(() => {     fetchOrders();   }, []);    const fetchOrders = async () => {     try {       const res = await axios.get('${API_URL}/api/orders');       setOrders(res.data);       setLoading(false);     } catch (err) {       console.error(err);       setLoading(false);     }   };    const parseTotal = (totalStr) => {     if (!totalStr) return 0;     return parseFloat(totalStr.replace(/[^0-9.]/g, '')) || 0;   };    // Metrics Calculation   const validOrders = orders.filter(o => o.status !== 'Cancelled');      const totalRevenue = validOrders.reduce((sum, order) => sum + parseTotal(order.total), 0);   const totalOrdersCount = validOrders.length;   const avgOrderValue = totalOrdersCount > 0 ? totalRevenue / totalOrdersCount : 0;    const today = new Date();   today.setHours(0, 0, 0, 0);   const todayOrders = validOrders.filter(o => new Date(o.createdAt) >= today);   const todayRevenue = todayOrders.reduce((sum, order) => sum + parseTotal(order.total), 0);    // Formatting   const formatCurrency = (amount) => {     return new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(amount);   };    const formatDate = (dateString) => {     return new Date(dateString).toLocaleDateString('en-US', {       year: 'numeric', month: 'short', day: 'numeric',       hour: '2-digit', minute: '2-digit'     });   };    if (loading) {     return <div className="text-white text-center py-20">Loading financial data...</div>;   }    return (     <div className="space-y-8 relative">       <div className="flex flex-col md:flex-row md:items-center justify-between gap-4">         <div>           <h1 className="text-3xl font-serif text-white mb-2">Finance Overview</h1>           <p className="text-gray-400 tracking-wide text-sm">Monitor your restaurant's financial performance and sales.</p>         </div>         <div className="flex items-center space-x-4">           <button className="bg-white/10 text-white px-6 py-3 rounded-lg flex items-center space-x-2 hover:bg-white/20 transition-colors uppercase tracking-widest text-sm font-semibold">             <Download size={18} />             <span className="hidden md:inline">Export Report</span>           </button>         </div>       </div>        {/* Metrics Cards */}       <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">         <div className="bg-[#111111] border border-white/5 rounded-2xl p-6 relative overflow-hidden group">           <div className="absolute top-0 right-0 p-4 opacity-10 group-hover:opacity-20 transition-opacity">             <DollarSign size={64} className="text-[#FFE600]" />           </div>           <div className="relative z-10">             <p className="text-gray-400 text-[10px] uppercase tracking-[0.2em] font-bold mb-1">Total Revenue</p>             <h3 className="text-3xl font-[900] text-white mb-2 tracking-tighter">{formatCurrency(totalRevenue)}</h3>             <div className="flex items-center text-emerald-400 text-[10px] font-bold uppercase tracking-widest">               <TrendingUp size={14} className="mr-1.5" />               <span>Lifetime earnings</span>             </div>           </div>         </div>          <div className="bg-[#111111] border border-white/5 rounded-2xl p-6 relative overflow-hidden group">           <div className="absolute top-0 right-0 p-4 opacity-10 group-hover:opacity-20 transition-opacity">             <Activity size={64} className="text-[#FFE600]" />           </div>           <div className="relative z-10">             <p className="text-gray-400 text-[10px] uppercase tracking-[0.2em] font-bold mb-1">Today's Revenue</p>             <h3 className="text-3xl font-[900] text-white mb-2 tracking-tighter">{formatCurrency(todayRevenue)}</h3>             <div className="flex items-center text-emerald-400 text-[10px] font-bold uppercase tracking-widest">               <TrendingUp size={14} className="mr-1.5" />               <span>Past 24 hours</span>             </div>           </div>         </div>          <div className="bg-[#111111] border border-white/5 rounded-2xl p-6 relative overflow-hidden group">           <div className="absolute top-0 right-0 p-4 opacity-10 group-hover:opacity-20 transition-opacity">             <CreditCard size={64} className="text-[#FFE600]" />           </div>           <div className="relative z-10">             <p className="text-gray-400 text-[10px] uppercase tracking-[0.2em] font-bold mb-1">Total Orders</p>             <h3 className="text-3xl font-[900] text-white mb-2 tracking-tighter">{totalOrdersCount}</h3>             <div className="flex items-center text-emerald-400 text-[10px] font-bold uppercase tracking-widest">               <ArrowUpRight size={14} className="mr-1.5" />               <span>Completed orders</span>             </div>           </div>         </div>          <div className="bg-[#111111] border border-white/5 rounded-2xl p-6 relative overflow-hidden group">           <div className="absolute top-0 right-0 p-4 opacity-10 group-hover:opacity-20 transition-opacity">             <TrendingUp size={64} className="text-[#FFE600]" />           </div>           <div className="relative z-10">             <p className="text-gray-400 text-[10px] uppercase tracking-[0.2em] font-bold mb-1">Avg Order Value</p>             <h3 className="text-3xl font-[900] text-white mb-2 tracking-tighter">{formatCurrency(avgOrderValue)}</h3>             <div className="flex items-center text-white/20 text-[10px] font-bold uppercase tracking-widest">               <span>Per transaction</span>             </div>           </div>         </div>       </div>        {/* Recent Transactions */}       <div className="bg-[#111111] border border-white/5 rounded-xl overflow-hidden">         <div className="border-b border-white/5 p-6 flex justify-between items-center">           <h2 className="text-xl font-serif text-white">Recent Transactions</h2>         </div>         <div className="p-0 overflow-x-auto">           <table className="w-full text-left border-collapse">             <thead>               <tr className="bg-black/50 text-gray-400 text-xs tracking-widest uppercase">                 <th className="px-6 py-4 font-normal">Order ID</th>                 <th className="px-6 py-4 font-normal">Date</th>                 <th className="px-6 py-4 font-normal">Customer</th>                 <th className="px-6 py-4 font-normal">Items</th>                 <th className="px-6 py-4 font-normal">Status</th>                 <th className="px-6 py-4 font-normal text-right">Amount</th>               </tr>             </thead>             <tbody className="text-sm">               {validOrders.slice(0, 15).map((order) => (                 <tr key={order._id} className="border-b border-white/5 hover:bg-white/5 transition-colors">                   <td className="px-6 py-4 text-gray-400 font-mono text-xs">#{order._id.toString().slice(-6)}</td>                   <td className="px-6 py-4 text-gray-300">{formatDate(order.createdAt)}</td>                   <td className="px-6 py-4">                     <span className="text-gray-200 font-medium">{order.customerName}</span>                   </td>                   <td className="px-6 py-4 text-gray-400">                     {order.items.length} item(s)                   </td>                   <td className="px-6 py-4">                      <span className={`px-4 py-1.5 rounded-full text-[10px] font-bold tracking-widest uppercase ${                         order.status === 'Delivered' ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20' :                          order.status === 'Pending' ? 'bg-[#FFE600]/10 text-[#FFE600] border border-[#FFE600]/20' :                         'bg-blue-500/10 text-blue-400 border border-blue-500/20'                       }`}>                         {order.status}                       </span>                   </td>                   <td className="px-6 py-4 text-right text-white font-medium">                     {formatCurrency(parseTotal(order.total))}                   </td>                 </tr>               ))}               {validOrders.length === 0 && (                 <tr>                   <td colSpan="6" className="px-6 py-12 text-center text-gray-500 text-sm">                     No transactions found.                   </td>                 </tr>               )}             </tbody>           </table>         </div>       </div>     </div>   ); };  export default FinanceManager;
