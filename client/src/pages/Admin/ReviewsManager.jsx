import { API_URL } from '../../utils/api';
import React, { useState, useEffect } from 'react'; import axios from 'axios'; import { Check, X, Trash2 } from 'lucide-react';  const ReviewsManager = () => {   const [reviews, setReviews] = useState([]);    useEffect(() => {     fetchReviews();   }, []);    const fetchReviews = async () => {     try {       const res = await axios.get('${API_URL}/api/reviews');       setReviews(res.data);     } catch (err) {       console.error(err);     }   };    const handleApproval = async (id, isApproved) => {     try {       await axios.put(`${API_URL}/api/reviews/${id}`, { isApproved });       fetchReviews();     } catch (err) {       console.error(err);     }   };    const handleDelete = async (id) => {     if (window.confirm('Are you sure you want to delete this review?')) {       try {         await axios.delete(`${API_URL}/api/reviews/${id}`);         fetchReviews();       } catch (err) {         console.error(err);       }     }   };    return (     <div className="space-y-8">       <div className="flex items-center justify-between">         <div>           <h1 className="text-3xl font-serif text-white mb-2">Review Management</h1>           <p className="text-gray-400 tracking-wide text-sm">Approve customer reviews to show on the frontend.</p>         </div>       </div>        <div className="bg-[#111111] border border-white/5 rounded-xl overflow-hidden">         <div className="p-0">           <table className="w-full text-left border-collapse">             <thead>               <tr className="bg-black/50 text-gray-400 text-xs tracking-widest uppercase">                 <th className="px-6 py-4 font-normal">Author</th>                 <th className="px-6 py-4 font-normal">Review</th>                 <th className="px-6 py-4 font-normal">Status</th>                 <th className="px-6 py-4 font-normal text-right">Actions</th>               </tr>             </thead>             <tbody className="text-sm">               {reviews.map((review) => (                 <tr key={review._id} className="border-b border-white/5 hover:bg-white/5 transition-colors group">                   <td className="px-6 py-4">                     <p className="text-gray-200 font-medium">{review.author}</p>                     <p className="text-gray-500 text-xs">{review.role}</p>                   </td>                   <td className="px-6 py-4 text-gray-400 max-w-md truncate">{review.content}</td>                   <td className="px-6 py-4">                     <span className={`px-3 py-1 rounded-full text-xs tracking-wide ${                       review.isApproved ? 'bg-emerald-500/10 text-emerald-500' : 'bg-amber-500/10 text-amber-500'                     }`}>                       {review.isApproved ? 'Approved' : 'Pending'}                     </span>                   </td>                   <td className="px-6 py-4 text-right">                     <div className="flex items-center justify-end space-x-3 opacity-0 group-hover:opacity-100 transition-opacity">                       {review.isApproved ? (                         <button onClick={() => handleApproval(review._id, false)} title="Hide from frontend" className="text-amber-500 hover:text-amber-400 transition-colors bg-amber-500/10 p-2 rounded-full">                           <X size={14} />                         </button>                       ) : (                         <button onClick={() => handleApproval(review._id, true)} title="Approve to frontend" className="text-emerald-500 hover:text-emerald-400 transition-colors bg-emerald-500/10 p-2 rounded-full">                           <Check size={14} />                         </button>                       )}                       <button onClick={() => handleDelete(review._id)} className="text-gray-400 hover:text-rose-400 transition-colors p-2 rounded-full">                         <Trash2 size={16} />                       </button>                     </div>                   </td>                 </tr>               ))}               {reviews.length === 0 && (                 <tr>                   <td colSpan="4" className="px-6 py-12 text-center text-gray-500 text-sm">                     No reviews found.                   </td>                 </tr>               )}             </tbody>           </table>         </div>       </div>     </div>   ); };  export default ReviewsManager;
